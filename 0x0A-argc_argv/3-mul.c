#include "main."
#include <stdio.h>

/**
 * main - multiplies two numbers
 * @a: number 1
 * @b: number 2
 * Return: Always 0 (Success)
 */
int main(int a, int b)
{
	int product;

	product = a * b;
	printf("%d", product)
}
return 0;
}#include "main."
#include <stdio.h>

/**
 * main - multiplies two numbers
 * @a: number 1
 * @b: number 2
 * Return: Always 0 (Success)
 */
int main(int a, int b)
{
	int product;

	product = a * b;
	printf("%d", product)
}
return 0;
}
